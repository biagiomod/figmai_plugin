{
  "name": "ace-lambda",
  "private": true,
  "version": "0.1.0",
  "description": "ACE Lambda API — private/work environment (JavaScript, modular)",
  "main": "handler.js",
  "scripts": {
    "bundle": "esbuild handler.js --bundle --platform=node --target=node20 --format=cjs --outfile=dist/handler.js --external:@aws-sdk/client-s3",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1000.0",
    "bcryptjs": "^2.4.3",
    "zod": "3.23.8"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  }
}
