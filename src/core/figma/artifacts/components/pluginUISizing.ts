/**
 * Centralized sizing constants for the FigmAI plugin UI stage preview.
 *
 * Source of truth: showUI({ width: 400, height: 600 }) in src/main.ts
 */

/** Plugin window width (px) as set by showUI(). */
export const PLUGIN_W = 400

/** Plugin window height (px) as set by showUI(). */
export const PLUGIN_H = 600

/** Confirm dialogs are narrower and centered within the plugin frame. */
export const CONFIRM_DIALOG_W = 320

/** Spacing between screen cards in the preview grid. */
export const CARD_GAP = 40

/** Gap between card label and the screen frame. */
export const LABEL_GAP = 12

/** Padding around the entire preview grid. */
export const GRID_PADDING = 24
