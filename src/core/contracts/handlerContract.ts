/**
 * Frozen Runtime Contract — Handler contract (re-exports)
 * Re-exports from handlers/base to avoid duplication. No behavior change.
 */

export type {
  HandlerContext,
  HandlerResult,
  AssistantHandler
} from '../assistants/handlers/base'
